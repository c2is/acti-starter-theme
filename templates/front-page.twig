{% extends "base.twig" %}

{% block content %}
    <div class="content-wrapper">
        <h1 class="article-h1">{{post.title}}</h1>
        <div class="article-body">
            {{ contentBuilder.contentHtml()|raw }}
        </div>
    </div>
{% endblock %}